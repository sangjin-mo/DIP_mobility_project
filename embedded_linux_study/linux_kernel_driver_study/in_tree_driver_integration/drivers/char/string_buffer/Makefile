# Keep the same tristate symbol in the parent and child Makefiles. If the
# parent descends with '=m', listing this object in obj-y would orphan it.
obj-$(CONFIG_STRING_BUFFER_DRIVER) += string_buffer.o
